# Тестирование исправления плейсхолдеров - Инструкция

## 🚀 Быстрая проверка исправления

### 1. Проверка API доступности
```bash
curl -s https://madlen.space/api/health | jq
```
**Ожидаемый результат:**
```json
{
  "status": "OK",
  "timestamp": "2025-07-15T11:10:44.301Z"
}
```

### 2. Веб-интерфейс тест
1. Перейдите: https://madlen.space/
2. Введите пароль: `admin12qw`
3. Выберите **"AI рекомендации"**
4. Выберите любое подразделение
5. **ВАЖНО**: Выберите **OpenAI** в качестве провайдера
6. Нажмите **"Запустить анализ"**

**Ожидаемый результат:**
- ✅ Агенты возвращают осмысленные рекомендации
- ❌ **НЕТ** сообщений "Пожалуйста, предоставьте данные для анализа"

### 3. Логи системы
```bash
# Просмотр логов AI системы
docker logs hr-miniapp -f | grep -E "MultiAgent"

# Поиск успешных замен плейсхолдеров
docker logs hr-miniapp | grep "✅ Все плейсхолдеры успешно обработаны"

# Поиск предупреждений
docker logs hr-miniapp | grep "⚠️ Найдены незамененные плейсхолдеры"
```

### 4. Автотесты
```bash
# Базовый тест замены плейсхолдеров
node test-placeholder-replacement.js

# Тест всех AI провайдеров
node test-ai-providers.js
```

**Ожидаемый результат:**
```
🎉 ВСЕ ТЕСТЫ ПРОШЛИ УСПЕШНО!
✅ Плейсхолдеры корректно заменяются для всех провайдеров
```

## 🔍 Что искать в логах

### ✅ Положительные сигналы:
```
[MultiAgent] 🔄 Заменено 3 плейсхолдеров: {forecast} → array[2] (101 символов)
[MultiAgent] ✅ Все плейсхолдеры успешно обработаны
[MultiAgent] Подготовленные данные для агента SalesAnalysisAgent: forecast: массив(2)
```

### ⚠️ Предупреждения (нормальны):
```
[MultiAgent] ⚠️ Найдены незамененные плейсхолдеры: {plan_vs_fact}
[MultiAgent] 🔄 Замена отсутствующих данных: {plan_vs_fact} → [Данные "plan_vs_fact" отсутствуют в MCP API]
```

### ❌ Критические ошибки (не должны появляться):
```
[MultiAgent] ❌ КРИТИЧЕСКАЯ ОШИБКА: Остались незамененные плейсхолдеры
```

## 📊 Результат исправления

**До исправления:**
- OpenAI возвращал: "Пожалуйста, предоставьте данные для анализа"
- Плейсхолдеры `{forecast}`, `{hourly_sales}` оставались незамененными

**После исправления:**
- ✅ Все плейсхолдеры заменяются на реальные данные
- ✅ Отсутствующие данные заменяются на информативные сообщения
- ✅ OpenAI и Claude работают корректно

## 🆘 Если что-то не работает

1. **Проверьте статус контейнеров:**
   ```bash
   docker-compose ps
   ```

2. **Перезапустите систему:**
   ```bash
   docker-compose restart
   ```

3. **Посмотрите полные логи:**
   ```bash
   docker logs hr-miniapp
   ```

4. **Проверьте автотесты:**
   ```bash
   node test-ai-providers.js
   ```